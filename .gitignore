node_modules/
dist/
*.tsbuildinfo

# Credentials & local state — must never be committed
.env
.env.*
config.toml
*.token.json
token.json
.cache/

# OS
.DS_Store

# 调研笔记与平台路由清单：本地保留，不入库
# （仓库已公开，这些是平台的材料，不对外分发）
docs/
